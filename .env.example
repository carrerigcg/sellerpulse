ML_CLIENT_ID=
ML_CLIENT_SECRET=
ML_USER_ID=
# OAuth redirect URI. Em dev local (rodando Streamlit direto), use a raiz do app:
#   http://localhost:8501/
# Em produção hospedada (Streamlit Cloud), use a URL pública HTTPS:
#   https://sellerpulse.streamlit.app/
# O valor precisa BATER exatamente com o Redirect URI registrado no portal do ML.
ML_REDIRECT_URI=http://localhost:8501/
